{
  "schema_version": 1,
  "description": "Proofshot visual verification config. PR authors include this file in the PR root to declare UI changes and define what proofshot should capture.",
  "fields": {
    "url": {
      "required": true,
      "description": "URL of the dev instance to capture (e.g., http://localhost:8788)",
      "default": "http://localhost:8788"
    },
    "seed": {
      "required": false,
      "description": "Path (relative to repo root) to a .js or .json seed file that populates localStorage before capture",
      "example": "scripts/seed.js"
    },
    "captures": {
      "required": false,
      "description": "Ordered list of captures. Each capture takes a screenshot. If omitted, proofshot defaults to 2 captures (initial + loaded).",
      "items": {
        "name": { "type": "string", "description": "Label for this capture step" },
        "selector": { "type": "string", "description": "CSS selector to scope the screenshot (omit for full-page)" },
        "wait": { "type": "integer", "description": "Milliseconds to wait before capturing" }
      }
    }
  },
  "example": {
    "url": "http://localhost:8788",
    "seed": "scripts/test-seed.js",
    "captures": [
      { "name": "initial", "wait": 2000 },
      { "name": "after-interaction", "selector": ".main-content", "wait": 1000 },
      { "name": "final-state", "wait": 500 }
    ]
  }
}
